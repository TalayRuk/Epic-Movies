
import { consoleLog } from 'epic-movies/helpers/console-log';
import { module, test } from 'qunit';

module('Unit | Helper | console log');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = consoleLog([42]);
  assert.ok(result);
});

